# Autoy 操作 SOP 與驗收任務

這份文件用來逐項確認使用者在頁面上的動作、Autoy 的判斷、送出的 API，以及預期結果。開始前先確認目前部署版本包含本 SOP 所述修正；本機開發檔修改不代表 GitHub Pages 已更新。

## A. 操作者開始前

1. 開啟 Autoy 網站。
2. 每個 token 對應獨立帳號項目；確認帳號別名後，可由帳號列各自啟動或停止。
3. 檢查該帳號自己的目標樓層、HP%、SP%、休息最短分鐘數與無動作提醒時間。
4. 頁面開啟後先自動讀取，前景頁面每 15 秒重新讀取目前帳號的角色與狩獵狀態；「重新讀取」可用於想立即更新時。檢查角色名單、勾選出戰數（1–4）、出戰角色 HP/SP、行動狀態、區域與樓層。
5. 如需更換 token，新增獨立帳號項目；不可把不同遊戲帳號共用同一筆設定。
6. 「顯示流程訊息」預設啟用，會在右側顯示目前帳號的讀取、休息、狩獵、死亡復原、等待與錯誤。日常使用看此區塊即可；可取消勾選隱藏。
7. 需要完整保留本次操作時，勾選「啟用操作紀錄」。每個 API 會記錄方法、路徑、request body、HTTP status、耗時與 response body，另含 runner 分支與排程事件；可清除或按「複製 JSON」交給開發者。它只保存在目前頁面，重新整理後清空；token、Authorization、Cookie、密碼與其他敏感欄位會遮罩。
8. 若要排查 API 細節，勾選「Console 除錯紀錄」，按 F12 開啟瀏覽器 DevTools 的 Console。紀錄會顯示 API 方法／路徑、隊伍快照、判斷分支、等待時間與錯誤；不會輸出 token。

## B. 按下「開始自動狩獵」後的啟動流程

| 順序 | Autoy 動作 | API／判斷 | 下一步 |
| --- | --- | --- | --- |
| 1 | 綁定此 runner 的帳號 ID，讀取角色與狩獵狀態 | 同時 `GET /heroes`、`GET /huntInfo` | 全程使用該 token；其他帳號 runner 可同時運行 |
| 2 | 篩選出戰名單 | 僅納入 `selected === true`；欄位必須是 boolean，出戰數量 1–4 | 0 名、超過 4 名或狀態不明時停止，不呼叫行動 API |
| 3 | 驗證回應完整性 | 出戰角色 HP/SP 上限為有效數字，行動狀態可識別 | 無效資料立即停止並顯示原因 |
| 4 | 檢查出戰角色死亡狀態 | `hp <= 0 && perished === false` 為死亡；`perished === true` 為死透 | 暫停狩獵，保留 runner 自動移動、重生／轉生並續戰 |
| 5 | 檢查出戰隊伍是否正在休息 | 任一出戰角色 `actionState === 2` | 進入 C「接續既有休息」；不得直接狩獵或重複呼叫 `restAll` |
| 6 | 檢查其他忙碌或未知行動 | 非休息時，所有出戰角色必須為 `actionState === 0` | 不符合即停止，要求先完成行動 |
| 7 | 計算出戰角色 HP/SP 百分比 | `hp / fullHp * 100`、`sp / fullSp * 100` | 全員達標才進入 E 狩獵；任何人未達標進入 D 休息 |

每次「啟動此帳號」都先重新抓取最新角色與狩獵資料，因此在停止期間於遊戲手動更換出戰角色後，重新啟動時會依最新 `selected === true` 的角色重新驗證，不會使用停止前的隊伍快照。

## B-1. 版面與訊息使用方式

1. 左側「角色」欄固定顯示目前帳號的所有角色，出戰／未出戰、HP/SP、位置和行動狀態都會隨自動重新讀取更新。
2. 右側是帳號、設定、開始／停止與死亡復原操作，避免在查看角色狀態時失去控制流程。
3. 右側下方的「流程訊息」按時間由新到舊顯示目前帳號的動作；可按「清除」清空畫面紀錄，不影響自動狩獵。
4. 流程訊息可按「複製訊息」複製可讀文字。取消「顯示流程訊息」只隱藏此面板，並不停止重新讀取或自動狩獵。
5. 「操作紀錄」預設關閉，勾選後才開始收集結構化事件。每一筆 `api.request` 對應一筆 `api.success` 或 `api.failure`，可從 `status` 與 `responseBody` 判斷伺服器是否成功處理；它適合比對一次自動化循環或提供問題紀錄。需要額外即時細節才開啟 Console 除錯。

## C. 啟動時已在休息：接續而非卡住

1. 先依 `/heroes` 的 `selected` 欄位篩出出戰角色，再判斷行動狀態；休息狀態為已觀察到的 `actionState: 2`。
2. 必須所有出戰角色都在休息狀態。部分出戰角色休息、部分空閒時，停止並提醒檢查；未勾選角色不參加隊伍行動判斷。
3. 對每位角色計算最早可完成時間：`max(actionCompleteTime, actionStart + 該帳號 restMinutes)`。等待至全隊最晚時間並加安全緩衝。
4. 時間到後重新讀取 `/heroes` 與 `/huntInfo`。如果 API 尚未回報所有休息角色 `canComplete: true`，維持等待並在 30 秒後重新檢查。
5. 只有全隊都 `canComplete: true` 時，呼叫一次 `POST /heroes/restAll/complete`。
6. 使用完成回應中的全隊角色狀態。若未確認全員回到 `actionState: 0`，停止，不可狩獵。
7. 重新計算全隊 HP/SP：
   - 全員達到該帳號門檻：進入 E 狩獵。
   - 任一人未達門檻：進入 D，再啟動一輪全隊休息。

**重要：**啟動時即使 HP/SP 已達門檻，也必須先完成伺服器中的休息行動，再狩獵；角色不能一邊休息一邊參戰。休息開始時間已由伺服器回傳時，不可把剩餘等待時間重設成完整休息分鐘數。

## D. 開始或重複全隊休息

| 順序 | Autoy 動作 | API／判斷 |
| --- | --- | --- |
| 1 | 確認出戰名單為 1–4 名、全員存活、資料有效且全員空閒 | 只檢查勾選出戰角色；任何不符狀態停止 |
| 2 | 啟動休息 | `POST /heroes/restAll`，無 request body |
| 3 | 使用伺服器時間排程 | 讀取每名出戰角色 `actionStart`、`actionCompleteTime`；等待時間不得早於伺服器允許時間 |
| 4 | 到期後重讀狀態 | `GET /heroes`、`GET /huntInfo` |
| 5 | 完成全隊休息 | 只有全員 `actionState: 2` 且 `canComplete: true` 時，呼叫一次 `POST /heroes/restAll/complete` |
| 6 | 驗證結果 | 完成後檢查所有出戰角色 `actionState: 0`，並重新計算每人的 HP/SP |
| 7 | 未達目標則再休息 | 任一出戰角色任一項低於目標，再開始一輪；全員達標才進入狩獵 |

## E. 狩獵、前行與冷卻

1. 每次狩獵前重新讀取 `/heroes` 與 `/huntInfo`。
2. 閘門條件：出戰角色 1–4 名、全員存活、`actionState: 0`、HP/SP 均達標、沒有未完成休息、伺服器冷卻已結束。未勾選角色不參與狩獵檢查。
3. 目前樓層低於目標且 `canForward: true`：呼叫一次 `POST /hunt?type=forward`。
4. 目前樓層等於目標：呼叫一次 `POST /hunt` 原地狩獵。
5. 樓層高於目標、不能前行、或行動回應不完整：停止並顯示原因，不猜測或自動後退。
6. 從回應更新角色、樓層、`canForward`、`huntAvailableAt`；下一輪必須重新讀取狀態。

## F. 回程／移動完成

| 步驟 | 操作與 API | 必須確認的狀態 |
| --- | --- | --- |
| 1 | 偵測到出戰角色死亡或死透；暫停此帳號所有狩獵／休息請求 | 僅依 `selected === true` 判斷；其他帳號 runner 不受影響 |
| 2 | 呼叫 `POST /move/0` 開始回程 | 讀取 `/heroes`、`/huntInfo`；保留伺服器回傳的 `actionCompleteTime` |
| 3 | 等待回程到期，重新讀取狀態；全隊出戰角色在移動且 `canComplete: true` 才呼叫 `POST /move/complete` | 不因計時器到期就盲目完成；每次寫入前先確認目前狀態 |
| 4a | 若完成後 `/huntInfo` 為大草原 `1/1` | 已在大草原，跳過 `POST /move/1`，進入步驟 7 |
| 4b | 若完成後 `/huntInfo` 為初始之鎮 `0/0`，呼叫 `POST /move/1` | 已觀察 HTTP 200；回應仍在城鎮 `0/0`，4 名英雄為 `actionState: 1`，`canBack/canForward: false`，共同完成時間為 `actionCompleteTime` |
| 5 | 等待 `/move/1` 的完成時間，再重新讀取 `/heroes`、`/huntInfo` | 只有全隊出戰角色都 `actionState: 1` 且 `canComplete: true` 才呼叫 `POST /move/complete` |
| 6 | 完成移動後再次讀取 `/heroes`、`/huntInfo`；遊戲 UI 的錄製接著有 `GET /zoneUsers` | 已觀察一筆完成回應為大草原 `1/1`、`canForward: true`、2 名英雄 `actionState: 0`；各次 recorder 的英雄數可能不同，以本次最新 API 狀態為準 |
| 7 | 確認大草原 `1/1` 後進入自動復原 | 一般死亡自動全部重生並逐位完成；死透角色依 `heroId` 自動逐角轉生 |
| 8 | 確認所有勾選出戰角色都已存活 | 回到 HP/SP 門檻檢查；達標後恢復前行／狩獵，不需手動重新啟動 |

若位置、移動狀態或完成時間無法確認，停止該帳號 runner 並顯示原因；不猜測位置、不呼叫狩獵 API。完整去識別化欄位見 `docs/Example/前往-大草原.md`、`docs/Example/完成移動.md`。

## G. 自動死亡復原與續戰

- 自動狩獵 runner 發現出戰角色死亡或死透時，暫停休息／狩獵請求，但保持 runner 執行並開始 F 節移動流程。
- 若 runner 啟動時，一般死亡角色已在 `actionState: 3` 重生中，先依完成時間等待並完成該重生，再從目前位置繼續回程／前往大草原；不可因頁面重載或啟動時狀態既存而要求人工重新啟動。
- 若角色正在移動，先依最新 `/heroes`、`/huntInfo` 與 `canComplete` 接續既有移動；不可重複送出相同移動 POST。
- 一般死亡（`hp <= 0`, `perished: false`）：呼叫 `POST /heroes/reviveAll` 開始重生。此為帳號批次 API；如果任何未勾選角色也一般死亡，必須停止並提示，避免復原未出戰角色。不得用批次端點盲目重試。
- 依各角色 `actionCompleteTime` 等待，重新讀取 `/heroes`；只有出戰一般死亡角色全為 `actionState: 3` 且 `canComplete: true` 才能逐位呼叫 `POST /heroes/{heroId}/completeAction`。每次完成後重讀最新狀態。
- 死透（`perished: true`）：只對該出戰角色呼叫 `POST /heroes/{heroId}/reincarnate`，每次一位；送出後立刻讀取 `/heroes` 確認狀態。結果仍顯示死透時停止，不自動重送。
- 混合死亡狀態時先逐角處理所有死透角色，再處理一般死亡；每個 POST 後重讀狀態，依最新資料繼續。
- 新偵測到的死亡隊伍按 F 節先移動到大草原再復原；只有啟動時已存在的重生行動會先完成，完成後接續移動。已在大草原 `1/1` 時略過多餘移動。
- 出戰角色全數存活後清除死亡復原階段，重新走完整 HP/SP、忙碌狀態與狩獵冷卻檢查，達標後自動恢復戰鬥。
- 任何 API 失敗、狀態不一致或寫入結果無法確認：停止該帳號 runner 並提示；使用者確認狀態後再啟動，不盲目重送。
- runner 停止時，畫面上的回程、全部重生與單角轉生按鈕仍可供手動補救；runner 運行且處理死亡時，改顯示自動復原進度並隱藏重複操作按鈕。

## H. 多帳號／多 token 操作

1. 每個遊戲帳號新增獨立帳號項目與 token；設定按帳號分開保存。
2. 每次請求使用目前帳號的 `token` header。成功回應若有新 token，只更新該帳號。
3. 多個帳號可各自同時運行；帳號列提供各自的啟動／停止按鈕，另有「停止全部帳號」。
4. 切換畫面帳號不會停止其他 runner；畫面只顯示目前帳號。每個 runner 固定使用自己的 account ID/token。
5. 一個帳號的英雄、出戰勾選、樓層、休息時間、冷卻或 token 不可套用到另一帳號。

## I. 操作者逐項驗收

- [ ] 選擇帳號後只看到該帳號角色，角色與位置讀取成功。
- [ ] 在遊戲手動啟動全隊休息但尚未完成時，Autoy 啟動後顯示休息中／完成時間，不送狩獵或重複休息請求。
- [ ] 若啟動時休息角色 HP/SP 已達目標，仍先完成休息，確認全隊空閒後才狩獵。
- [ ] 若啟動時休息完成後仍有人未達目標，Autoy 再做完整休息循環；達標前不送狩獵。
- [ ] 設定休息中途到期時間：未達伺服器時間或 `canComplete` 未全員成立時，不送 `restAll/complete`。
- [ ] 休息狀態不一致、未知 actionState、空英雄清單或 HP/SP 上限無效時，Autoy 停止並顯示原因。
- [ ] 目標樓層低於／等於／高於目前樓層時，分別驗證前行／原地狩獵／停止。
- [ ] 分別勾選 0、1、4、5 名角色；只有 1–4 名時可進入狩獵，5 名時不得送出任何行動 POST。
- [ ] 未勾選的低 HP、死亡或忙碌角色不參加狩獵隊伍檢查。
- [ ] 角色死亡與死透時，驗證暫停狩獵並自動移動、重生／轉生；存活後恢復 HP/SP 檢查與狩獵。
- [ ] runner 啟動時若一般死亡角色已在 `actionState: 3`，驗證會先完成重生，再接續回程／前往大草原與狩獵，不要求再次手動啟動。
- [ ] runner 啟動時若隊伍已在移動，驗證依伺服器狀態接續等待／完成，不重複送出移動 POST。
- [ ] 一般死亡依伺服器完成時間自動等待；未全員 `canComplete: true` 前不送完成行動；全部完成後恢復狩獵。
- [ ] 死透角色只送對應 `heroId` 的轉生請求；成功後確認角色狀態；寫入結果不明不得自動重送。
- [ ] 未出戰角色有一般死亡時，不呼叫帳號批次 `reviveAll`，顯示停止原因。
- [ ] 自動復原遇到 API 失敗、狀態不一致或寫入結果無法確認時，只停止該帳號 runner 並顯示原因；確認狀態後才手動重啟。
- [ ] 全隊未到 `canComplete: true` 前不送 `POST /move/complete`；移動端點失敗或位置不符，停止且不狩獵。
- [ ] 啟動兩個 token 的 runner 後，兩邊各自運行；切換畫面時狀態、事件與角色資料不串用。
- [ ] 停止一個 token 時，另一個 token 繼續；「停止全部帳號」才會全部停止。
- [ ] 停止按鈕生效後，不再送出下一個遊戲行動請求。
- [ ] 停止 runner、在遊戲手動更換出戰角色後再啟動，驗證新的 `selected` 名單重新通過 1–4 名、存活、HP/SP 與行動狀態檢查。
- [ ] 勾選「Console 除錯紀錄」後，Console 有 `api.request`、`state.refreshed`、`runner.branch`、`runner.scheduled` 與失敗時的 `runner.error`；輸出中不得含 token。
- [ ] 勾選「啟用操作紀錄」後，執行一次重新讀取或自動回合，操作紀錄有 `api.request`、`api.success`／`api.failure`、耗時與 runner 事件；關閉前的事件不補記。
- [ ] 操作紀錄可「複製 JSON」與清除；流程訊息可「複製訊息」與清除；操作紀錄每筆 API 要有 requestBody、status、durationMs 與 responseBody，且不得含 token、Authorization、Cookie、密碼或其他敏感欄位。
- [ ] 在頁面停留至少 15 秒，角色欄與目前帳號狀態會自動重新讀取；不需按「重新讀取」。
- [ ] 「顯示流程訊息」啟用時可看見目前帳號的執行步驟；停用時面板隱藏，重新讀取與 runner 仍正常執行。

## J. 已觀察 API 清單

| 操作 | 方法與端點 |
| --- | --- |
| 讀角色 | `GET /heroes` |
| 讀狩獵狀態 | `GET /huntInfo` |
| 啟動全隊休息 | `POST /heroes/restAll` |
| 完成全隊休息 | `POST /heroes/restAll/complete` |
| 原地狩獵 | `POST /hunt` |
| 前行並狩獵 | `POST /hunt?type=forward` |
| 開始回程 | `POST /move/0` |
| 前往大草原 | `POST /move/1` |
| 完成移動 | `POST /move/complete` |
| 讀取區域玩家 | `GET /zoneUsers` |
| 全部重生 | `POST /heroes/reviveAll` |
| 個別轉生 | `POST /heroes/{heroId}/reincarnate` |
