# Autoy 操作 SOP 與驗收任務

這份文件用來逐項確認使用者在頁面上的動作、Autoy 的判斷、送出的 API，以及預期結果。開始前先確認目前部署版本包含本 SOP 所述修正；本機開發檔修改不代表 GitHub Pages 已更新。

## A. 操作者開始前

1. 開啟 Autoy 網站。
2. 選取要操作的帳號；帳號別名必須對應正確的遊戲 token。
3. 檢查該帳號自己的目標樓層、HP%、SP%、休息最短分鐘數與無動作提醒時間。
4. 按「重新讀取」，檢查角色數量、角色 HP/SP、行動狀態、所在區域與樓層。
5. 如需更換 token，新增獨立帳號項目；不可把不同遊戲帳號共用同一筆設定。

## B. 按下「開始自動狩獵」後的啟動流程

| 順序 | Autoy 動作 | API／判斷 | 下一步 |
| --- | --- | --- | --- |
| 1 | 鎖定目前帳號 ID，讀取最新角色與狩獵狀態 | 同時 `GET /heroes`、`GET /huntInfo` | 只處理目前帳號；切換帳號後不沿用舊角色資料 |
| 2 | 驗證回應完整性 | 角色清單不可空；HP、SP、上限需為有效數字；行動狀態需可識別 | 無效資料立即停止並顯示原因 |
| 3 | 檢查死亡狀態 | `hp <= 0 && perished === false` 為死亡；`perished === true` 為死透 | 停止狩獵，顯示回程、重生或個別轉生可用操作 |
| 4 | 檢查全隊是否正在休息 | 觀察到 `actionState === 2` | 進入 C「接續既有休息」；不得直接狩獵，也不得重複呼叫 `restAll` |
| 5 | 檢查其他忙碌或未知行動 | 非休息時，所有角色必須為 `actionState === 0` | 不符合即停止，要求先完成移動或其他行動 |
| 6 | 計算每位角色 HP/SP 百分比 | `hp / fullHp * 100`、`sp / fullSp * 100` | 全員達標才進入 E 狩獵；任何人未達標進入 D 休息 |

## C. 啟動時已在休息：接續而非卡住

1. 使用啟動時取得的 `/heroes` 判斷行動狀態；休息狀態為已觀察到的 `actionState: 2`。
2. 必須全隊都在休息狀態。部分角色休息、部分角色空閒時，停止並提醒檢查，不呼叫任何狩獵 API。
3. 對每位角色計算最早可完成時間：`max(actionCompleteTime, actionStart + 該帳號 restMinutes)`。等待至全隊最晚時間並加安全緩衝。
4. 時間到後重新讀取 `/heroes` 與 `/huntInfo`。如果 API 尚未回報所有休息角色 `canComplete: true`，維持等待並在 30 秒後重新檢查。
5. 只有全隊都 `canComplete: true` 時，呼叫一次 `POST /heroes/restAll/complete`。
6. 使用完成回應中的全隊角色狀態。若未確認全員回到 `actionState: 0`，停止，不可狩獵。
7. 重新計算全隊 HP/SP：
   - 全員達到該帳號門檻：進入 E 狩獵。
   - 任一人未達門檻：進入 D，再啟動一輪全隊休息。

**重要：**啟動時即使 HP/SP 已達門檻，也必須先完成伺服器中的休息行動，再狩獵；角色不能一邊休息一邊參戰。休息開始時間已由伺服器回傳時，不可把剩餘等待時間重設成完整休息分鐘數。

## D. 開始或重複全隊休息

| 順序 | Autoy 動作 | API／判斷 |
| --- | --- | --- |
| 1 | 確認全隊存活、資料有效且全員空閒 | 任何不符狀態停止 |
| 2 | 啟動休息 | `POST /heroes/restAll`，無 request body |
| 3 | 使用伺服器時間排程 | 讀取每名角色 `actionStart`、`actionCompleteTime`；等待時間不得早於伺服器允許時間 |
| 4 | 到期後重讀狀態 | `GET /heroes`、`GET /huntInfo` |
| 5 | 完成全隊休息 | 只有全員 `actionState: 2` 且 `canComplete: true` 時，呼叫一次 `POST /heroes/restAll/complete` |
| 6 | 驗證結果 | 完成後檢查全隊 `actionState: 0`，並重新計算每人的 HP/SP |
| 7 | 未達目標則再休息 | 任一角色任一項低於目標，再開始一輪；全員達標才進入狩獵 |

## E. 狩獵、前行與冷卻

1. 每次狩獵前重新讀取 `/heroes` 與 `/huntInfo`。
2. 閘門條件：角色清單非空、所有角色存活、所有角色 `actionState: 0`、HP/SP 均達標、全隊沒有未完成休息、伺服器冷卻已結束。
3. 目前樓層低於目標且 `canForward: true`：呼叫一次 `POST /hunt?type=forward`。
4. 目前樓層等於目標：呼叫一次 `POST /hunt` 原地狩獵。
5. 樓層高於目標、不能前行、或行動回應不完整：停止並顯示原因，不猜測或自動後退。
6. 從回應更新角色、樓層、`canForward`、`huntAvailableAt`；下一輪必須重新讀取狀態。

## F. 回程／移動完成

1. 偵測到死亡或死透角色時，自動狩獵停止。
2. 使用者按「回程／返回起點」並確認後，呼叫 `POST /move/0`。
3. 依回應顯示目前位置；此請求曾回傳大草原第 1 層，不能保證直接回城。
4. 移動期間按回傳的 `actionCompleteTime` 等待並重新讀取。
5. 最新角色資料中移動角色為 `actionState: 1 && canComplete: true` 時，顯示「完成移動」。
6. 使用者按下後呼叫一次 `POST /move/complete`，再讀取 `/heroes` 與 `/huntInfo`。目的地以實際回應為準；已有紀錄分別到初始之鎮 0/0 與大草原 1/1。
7. 移動完成後不自動恢復狩獵；使用者檢查狀態後再按「開始」。

## G. 死亡／復原操作

- 一般死亡（`hp <= 0`, `perished: false`）：停止自動化；使用者可確認回程或「全部重生」。重生開始使用 `POST /heroes/reviveAll`，之後依 `actionCompleteTime` 重讀；單角色 `canComplete: true` 才能按完成。
- 死透（`perished: true`）：停止自動化；單角色「轉生」送出前二次確認，呼叫 `POST /heroes/{heroId}/reincarnate`，完成後重讀全隊。
- 任何寫入請求結果不明：不可自動重送；先重新讀取狀態並交由使用者確認。

## H. 多帳號／多 token 操作

1. 每個遊戲帳號新增獨立帳號項目與 token；設定按帳號分開保存。
2. 每次請求使用目前帳號的 `token` header。成功回應若有新 token，只更新該帳號。
3. 目前只允許一個帳號執行；切換帳號會停止原執行器、清除畫面英雄與排程，再讀取新帳號。
4. 一個帳號的英雄、樓層、休息時間、冷卻或 token 不可套用到另一帳號。

## I. 操作者逐項驗收

- [ ] 選擇帳號後只看到該帳號角色，角色與位置讀取成功。
- [ ] 在遊戲手動啟動全隊休息但尚未完成時，Autoy 啟動後顯示休息中／完成時間，不送狩獵或重複休息請求。
- [ ] 若啟動時休息角色 HP/SP 已達目標，仍先完成休息，確認全隊空閒後才狩獵。
- [ ] 若啟動時休息完成後仍有人未達目標，Autoy 再做完整休息循環；達標前不送狩獵。
- [ ] 設定休息中途到期時間：未達伺服器時間或 `canComplete` 未全員成立時，不送 `restAll/complete`。
- [ ] 休息狀態不一致、未知 actionState、空英雄清單或 HP/SP 上限無效時，Autoy 停止並顯示原因。
- [ ] 目標樓層低於／等於／高於目前樓層時，分別驗證前行／原地狩獵／停止。
- [ ] 角色死亡與死透時，驗證自動狩獵停止且不呼叫休息或狩獵端點。
- [ ] 切換兩個 token 時，各自讀到正確英雄、位置與設定；不串用狀態。
- [ ] 停止按鈕生效後，不再送出下一個遊戲行動請求。

## J. 已觀察 API 清單

| 操作 | 方法與端點 |
| --- | --- |
| 讀角色 | `GET /heroes` |
| 讀狩獵狀態 | `GET /huntInfo` |
| 啟動全隊休息 | `POST /heroes/restAll` |
| 完成全隊休息 | `POST /heroes/restAll/complete` |
| 原地狩獵 | `POST /hunt` |
| 前行並狩獵 | `POST /hunt?type=forward` |
| 開始回程 | `POST /move/0` |
| 完成移動 | `POST /move/complete` |
| 全部重生 | `POST /heroes/reviveAll` |
| 個別轉生 | `POST /heroes/{heroId}/reincarnate` |
